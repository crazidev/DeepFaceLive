# Copy to ".env" in the project root and load before starting, e.g.:
#   set -a && source .env && set +a   # bash/zsh
# DFL_LITE=1  →  same as --lite (sources + preview only; skip face ONNX stack)

DFL_STREAM_BIND_HOST=0.0.0.0
DFL_STREAM_CLIENT_HOST=
DFL_STREAM_PORT_UDP=1238
DFL_STREAM_PORT_SRT=8888
DFL_STREAM_PORT_RTMP=1935
DFL_STREAM_PORT_RTSP=8554
DFL_STREAM_PROTOCOL=udp
