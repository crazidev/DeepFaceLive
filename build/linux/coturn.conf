# Template reference — production config is generated in entrypoint.sh from DFL_TURN_* env vars.
# TURNS listens on TCP 443 for RunPod (no UDP port expose required).

listening-port=3478
tls-listening-port=443
listening-ip=0.0.0.0
relay-ip=0.0.0.0
fingerprint
lt-cred-mech
no-multicast-peers
no-cli
